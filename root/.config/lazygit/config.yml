os:
  editPreset: "nvim-remote"
gui:
  nerdFontsVersion: "3"
# The theme is layered on top via LG_CONFIG_FILE (set in .zshrc) so it can
# follow the system appearance: theme-tokyonight.yml (dark) / theme-latte.yml
# (light). See root/.config/lazygit/ for those overlays.
